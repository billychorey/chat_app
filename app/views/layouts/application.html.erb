<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Chat App" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body data-logged-in="<%= logged_in? %>">
    <header>
    </header>
    <main>
      <%= yield %>
      <div id="react-root">"test"</div>

    </main>
    <footer>
      <p>&copy; <%= Date.today.year %> Chat App</p>
    </footer>
  </body>
</html>
